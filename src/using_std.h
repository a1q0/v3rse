#pragma once

#include <iostream>
#include <stdexcept>
#include <cstring>
#include <cstdlib>
#include <vector>
#include <optional>
#include <set>
#include <map>
#include <array>

using std::vector;
using std::array;
using std::optional;
using std::set;
using std::map;
using std::multimap;
using std::make_pair;
using std::cout;
using std::endl;

#include <chrono>

typedef std::chrono::high_resolution_clock Clock;
